{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello  HomeController ! ✅</h1>

    {% for article in articles %}
        <div style='width:200px;height:300px;border:1px solid gray;margin:20px;padding:10px;display:inline-block;'>
            <p><img src="{{ asset('public/upload/') ~ article.image}}" style='wisth:80px;height:80px;'/></p>
            <p>{{article.title}}</p>
            <p>{{article.category}}</p>
            <p>{{article.summery}}</p>
            <p>{{article.description}}</p>
        </div>
    {% endfor %}
</div>
{% endblock %}
